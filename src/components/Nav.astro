---
import karmotLogo from '../assets/karmot-logo.svg';
---

<nav class="nav" id="nav">
  <a class="nav__brand" href="/" aria-label="Karmot ana sayfa">
    <img src={karmotLogo.src} alt="Karmot Güç Aktarım Sistemleri" width="156" height="37" />
  </a>
  <div class="nav__links">
    <a class="mono-label nav__link" href="#zincir">Güç Zinciri</a>
    <a class="mono-label nav__link" href="#guven">Neden Karmot</a>
    <a class="mono-label nav__link" href="#iletisim">İletişim</a>
  </div>
  <a class="btn nav__cta" href="#iletisim">Teklif Al</a>
</nav>

<style>
  .nav {
    position: fixed;
    inset: 0 0 auto 0;
    z-index: 800;
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 0.85rem var(--gutter);
    transition: background 0.4s ease, box-shadow 0.4s ease;
  }
  .nav.is-scrolled {
    background: color-mix(in srgb, var(--navy-950) 82%, transparent);
    backdrop-filter: blur(12px);
    box-shadow: 0 1px 0 var(--line);
  }
  .nav__brand img {
    height: 34px;
    width: auto;
    filter: brightness(0) invert(1);
  }
  .nav__links {
    display: flex;
    gap: 1.8rem;
    margin-left: auto;
  }
  .nav__link {
    color: var(--text-dim);
    transition: color 0.25s ease;
  }
  .nav__link:hover { color: #fff; }
  .nav__cta {
    padding: 0.65em 1.4em;
    font-size: 0.8rem;
  }
  @media (max-width: 760px) {
    .nav__links { display: none; }
    .nav__cta { margin-left: auto; }
  }
</style>
